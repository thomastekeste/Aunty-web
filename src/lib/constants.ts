/** Shared marketing constants — single source of truth for key figures */
export const FOUNDING_SPOTS = 500;

/**
 * Yearly vs monthly savings.
 * Yearly = $9.99, Monthly × 12 = $11.88 → saves $1.89
 * Keep in sync with PLANS in plans.ts if prices change.
 */
export const YEARLY_SAVINGS_DISPLAY = "$1.89";
